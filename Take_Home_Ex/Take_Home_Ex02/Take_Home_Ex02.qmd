---
title: "Take_Home Ex02: Singapore Bus Mobility and Urban Development Patterns"
author: "Pwint Phoo Thaw"
date: "October 23, 2025"
date-modified: "October 23, 2025"
number-sections: true
execute: 
  eval: true
  echo: true
  warning: false
  message: false
  freeze: true
---

![](images/SG%20Bus%20&%20MBS.jpg)

## 1. Overview

Buses serve as a vital component of Singapore’s public transport network, connecting residential neighbourhoods, business hubs, and peripheral regions. To support the nation’s long-term mobility planning, this project conducts a comprehensive spatio-temporal analysis of commuter behaviour, moving beyond traditional mapping toward data-driven transport planning. By leveraging large-scale bus tap-on data, the study examines not only the intensity and distribution of passenger flows but also their spatial clustering and temporal dynamics.

The analysis is structured in three sections:

-   **Spatial Distribution Analysis**\
    Passenger trips are aggregated into a hexagonal Traffic Analysis Zone (TAZ) framework to build a clear spatial representation of mobility intensity during weekday morning, weekday evening, and weekend periods. This foundational layer visualises baseline travel flows and spatial distribution patterns across the island.

-   **Spatial Clustering**\
    To detect localised patterns of demand, the study applies Local Measures of Spatial Autocorrelation (LMSA) — including *Local Moran’s I*,

-   **Temporal Trend Analysis**\
    To examine how these clusters evolve, Emerging Hot Spot Analysis (EHSA) integrates the Gi\* statistic with the Mann-Kendall trend test, classifying clusters as emerging, intensifying, diminishing, or sporadic over time. This captures the dynamic dimension of urban mobility.

### 1.1 Datasets

**Aspatial Data**

-   *Passenger Volume by Origin Destination Bus Stops* from [LTA DataMall](https://datamall.lta.gov.sg/content/datamall/en.html)

**Geospatial Data**

-   *Bus Stop Location* from LTA DataMall. It provides information about all the bus stops currently being serviced by buses, including the bus stop code (identifier) and location coordinates.

-   *Master Plan 2019 Subzone Boundary (No Sea)* from [Singapore’s open data portal](https://data.gov.sg/).

## 2. Data Preparation

### 2.1 Loading the R packages

The following R packages are used:

-   

    | R Package | Description |
    |------------------------------------|------------------------------------|
    | **sf** | A package that provides [**simple features access**](https://en.wikipedia.org/wiki/Simple_Features) for R. |
    | **spdep** | Creates spatial weights matrices and to test for and model spatial autocorrelation in geographical data, like Moran’s I and Geary’s C and Local Moran’s I and Getis-Ord G. |
    | **tidyverse** | Includes the core packages like ggplot2, dplyr, readr, purrr, tibble, lubridate, etc. |
    | **sfdep** | Builds on the great shoulders of **spdep** package for spatial dependence. sfdep creates an sf and tidyverse friendly interface to the package as well as introduces new functionality that is not present in spdep. |
    | **knitr** | Color maps designed to improve graph readability for readers with common forms of color blindness and/or color vision deficiency. |
    | **tmap** | geographical maps in which spatial data distributions are visualized. This package offers a flexible, layer-based, and easy to use approach to create thematic maps, such as choropleths and bubble maps. |
    | **rvest** | Provides the HTML parsing and extraction functions |
    | **patchwork** |  |

```{r}

pacman::p_load(tidyverse, sf, spdep, sfdep, knitr,tmap, rvest, patchwork,plotly, Kendall)
```

### 2.2 Importing Data

#### 2.2.1 Bus Stop Location

We need to transform the data for EPSG:3414 Projected coordinate system for Singapore as bus Stop location data has crs = 9001,

```{r}

BusStop = st_read(dsn = "data/LTADataMall/",
                  layer = "BusStop") %>%
  st_transform(crs = 3414)
```

#### 2.2.2 Master Plan 2019 Subzone

```{r}
mpsz_sf <- st_read("data/geospatial/MasterPlan2019SubzoneBoundaryNoSeaKML.kml") %>%
  st_zm(drop = TRUE, what = "ZM") %>% st_transform(crs = 3414)
```

```{r}
plot(mpsz_sf)
```

```{r}
extract_kml_field <- function(html_text, field_name) {
  if (is.na(html_text) || html_text == "") return(NA_character_)
  
  page <- read_html(html_text)
  rows <- page %>% html_elements("tr")
  
  value <- rows %>%
    keep(~ html_text2(html_element(.x, "th")) == field_name) %>%
    html_element("td") %>%
    html_text2()
  
  if (length(value) == 0) NA_character_ else value
}
```

```{r}
mpsz_sf <- mpsz_sf %>%
  mutate(
    REGION_N = map_chr(Description, extract_kml_field, "REGION_N"),
    PLN_AREA_N = map_chr(Description, extract_kml_field, "PLN_AREA_N"),
    SUBZONE_N = map_chr(Description, extract_kml_field, "SUBZONE_N"),
    SUBZONE_C = map_chr(Description, extract_kml_field, "SUBZONE_C")
  ) %>%
  select(-Name, -Description) %>%
  relocate(geometry, .after = last_col())
```

```{r}
mpsz_cl <- mpsz_sf %>%
  filter(SUBZONE_N != "SOUTHERN GROUP",
         PLN_AREA_N != "WESTERN ISLANDS", 
         PLN_AREA_N != "NORTH-EASTERN ISLANDS")
```

```{r}
write_rds(mpsz_cl, 
          "data/rds/mpsz_cl.rds")
```

#### **2.2.2.1MRT Station**

Below we will import **Master Plan 2019 Subzone Boundary (No Sea).**

```{r}
mrt <- st_read("data/LTADataMall/LTAMRTStationExitKML.kml") %>%
  st_zm(drop = TRUE, what = "ZM") %>%
  st_transform(crs = 3414)
```

Below code is to write MRT station data into a rsd file, so we can directly call it without processing the data set again.

```{r}
#| eval: false
extract_kml_field <- function(html_text, field_name) {
  if (is.na(html_text) || html_text == "") return(NA_character_)
  
  page <- read_html(html_text)
  rows <- page %>% html_elements("tr")
  
  value <- rows %>%
    keep(~ html_text2(html_element(.x, "th")) == field_name) %>%
    html_element("td") %>%
    html_text2()
  
  if (length(value) == 0) NA_character_ else value
}

mrt_sf <- mrt %>%
  mutate(
    STATION = map_chr(Description, extract_kml_field, "STATION_NA"),
    EXIT_CODE = map_chr(Description, extract_kml_field, "EXIT_CODE"),
    INC_CRC = map_chr(Description, extract_kml_field, "INC_CRC"),
    FMEL_UPD_D = map_chr(Description, extract_kml_field, "FMEL_UPD_D")
  ) %>%
  
  # Just one exit for each station to avoid multiple points.
  filter(EXIT_CODE == "Exit A") %>%
  
  select(-Name, -Description) %>%
  relocate(geometry, .after = last_col())


write_rds(mrt_sf, "data/rds/mrt_sf.rds")
```

```{r}
mrt_sf <- read_rds("data//rds/mrt_sf.rds")
```

Plot the MRT station exits in a tmap.

```{r}
tmap_mode("view")
tm_shape(mpsz_cl) +
  tm_polygons(fill = "#A1C2BD", filll_alpha = 0.3) +
  tm_text("SUBZONE_N", size = 0.4) +
tm_shape(mrt_sf) +
  tm_symbols(
   fill = "#EDA35A", size = 0.4,
   col = "#D9E9CF",
#   popup = "STATION"
  ) +
  tm_layout(
    main.title = "MRT Station Exits",
    legend.show = FALSE    # Hide the legend since there are many unique stations
  )

```

#### 2.2.3 Origin Destination Bus Trips

```{r}
odbus <- read_csv("data/LTADataMall/origin_destination_bus_202508.csv")
```

```{r}
# Import and combine two months of bus data
odbus_aug <- read_csv("data/LTADataMall/origin_destination_bus_202508.csv")
odbus_sep <- read_csv("data/LTADataMall/origin_destination_bus_202509.csv")

# Combine with month identifier
odbus_combined <- bind_rows(
  odbus_aug %>% mutate(MONTH = "2025-08", MONTH_NUM = 8),
  odbus_sep %>% mutate(MONTH = "2025-09", MONTH_NUM = 9)
) %>%
  select(MONTH, MONTH_NUM, everything())

cat("Combined dataset contains", nrow(odbus_combined), "records across", 
    n_distinct(odbus_combined$MONTH), "months\n")
```

##### 2.2.3.1 Visualise the Bus Stops

```{r}

#| fig-width: 12
#| fig-height: 8

tm_shape(mpsz_cl) +
  tm_polygons() +
  tm_shape(BusStop) +
  tm_dots(size = 0.15, fill = "black", col = "grey") +
  tm_layout(bg.color = "lightblue",
            outer.margins = c(0.01, 0.01, 0.01, 0.01))
```

##### 2.2.3.2 Extract Bus Stops within Singapore

Some bus stops are outside the polygons because of JB border and they have to be excluded**.** One way to exclude them is to plot the mpsz layer on top of the BusStop layer.

```{r}
BusStop_in_SG <- st_join(
  BusStop, mpsz_cl,
  join = st_within,
  left = FALSE)
```

Plot the bus stop data again to check that the bus stops have been removed.

```{r}
#| fig-width: 12
#| fig-height: 8

tm_shape(mpsz_cl) +
  tm_polygons() +
  tm_shape(BusStop_in_SG) +
  tm_dots(size = 0.2, fill = "black", col = "grey") +
  tm_layout(main.title = "Bus Stops Within Singapore Boundaries",
            bg.color = "lightblue",
            outer.margins = c(0.01, 0.01, 0.01, 0.01))
```

##### 2.2.3.3 Deriving Analytical Hexagon

```{r}
hexagon <- st_make_grid(mpsz_cl,
                        cellsize = 750,
                        what = "polygon",
                        square = FALSE) %>%
  st_sf()
```

##### 2.2.3.4 Filtering hexagons with bus stops

To retain hexagons with bus stops. Use `st_intersects()` to return 2 values: True, False (0 or 1). The hexagon with a bus stop would be 1, and without a bus stop would return 0.

```{r}
hexagon$n_collisions =
  lengths(st_intersects(
    hexagon, BusStop_in_SG))

hexagon <- filter(hexagon, n_collisions > 0)
```

```{r}
#| fig-width: 12
#| fig-height: 8
tm_shape(mpsz_cl) +
  tm_polygons() +
  tm_shape(hexagon) +
  tm_fill(col = "grey20", fill = "orange", fill_alpha = 0.7) +
  tm_layout(bg.color = "lightblue",
            outer.margins = c(0.01, 0.01, 0.01, 0.01))
```

The hexagon map shows where bus stops are found across Singapore. Each colored hexagon marks an area with at least one bus stop, making it easy to see that most bus stops are located in built-up towns and along main roads. Many other parts of Singapore, especially less developed areas, don’t have direct bus service. This map gives us a clear base to group and analyze passenger numbers later. Since the hexagons don’t have names or numbers, we’ll give each one an ID so we can keep track of them more easily.

##### 2.2.3.5 Assigning ID to each hexagon

In order to know the trips occurred in each hexagon in certain time periods, we need to give each hexagon an ID.

```{r}
hexagon <- hexagon %>%
  select(,-n_collisions)

hexagon$HEX_ID <- sprintf(
  "H%04d", seq_len(nrow(hexagon))) %>%
  as.factor()

kable(head(hexagon))
```

#### 2.2.4 Preparing Trip Generation Data

We are only interested in ORIGIN_PT_CODE (trip origin).

##### 2.2.4.1 Cleaning the data

Only necessary columns are selected.

```{r}
trips <- odbus_combined %>%
  select(c(MONTH, MONTH_NUM,ORIGIN_PT_CODE, DAY_TYPE, TIME_PER_HOUR, TOTAL_TRIPS)) %>%
  rename(BUS_STOP_N = ORIGIN_PT_CODE) %>%
  rename(HOUR_OF_DAY = TIME_PER_HOUR) %>%
  rename(TRIPS = TOTAL_TRIPS)
trips$BUS_STOP_N <- as.factor(trips$BUS_STOP_N)
kable(head(trips))
```

##### 2.2.4.2 Combining bus stop and hexagon layers

```{r}
bs_hex <-st_intersection(
  BusStop, hexagon) %>%
  st_drop_geometry() %>%
  select(c(BUS_STOP_N, HEX_ID))
kable(head(bs_hex))
```

##### 2.2.4.3 Adding Hex_ID into bus trips data

Use inner_join() to join trips and bus_hex

```{r}
trips <- inner_join(trips, bs_hex)
kable(head(trips))
```

```{r}
trips_sort <- trips %>%
  arrange(desc(TRIPS)) %>%
  mutate(HEX_ID = factor(HEX_ID, levels = unique(HEX_ID)))
```

```{r}
glimpse(trips_sort)
```

```{r}
ggplot(trips_sort,
  aes(x = HEX_ID, y = TRIPS, fill = HEX_ID)) +
  geom_col(fill = "blue") +
  labs(title = "Total Trips by HexID",
       x = "Hex ID", y = "Number of Trips") + 
  theme_minimal()
```

#### 2.2.5 Create Total Trips per Hexagon dataset

*Hexagon* spatial data and *trips* aspatial data will be joined for Local Measures of Spatial Association (LMSA) analysis. This is to allow us to view the spatial distribution of total bus trips.

##### 2.2.5.1 Aggregate Trip Data by Hexagon

Within *trips* dataset, we calculate the total TRIPS per hexagon ("HEX_ID").

```{r}
trips_agg <- trips %>%
  group_by(HEX_ID) %>%
  summarise(total_trips = sum(TRIPS, na.rm = TRUE),
            .groups = 'drop')

kable(head(trips_agg))
```

##### 2.2.5.2 Relational Join

Join *trips_agg* with *hexagon* on "HEX_ID".

```{r}
hexagon_trips <- hexagon %>%
  inner_join(trips_agg, by = "HEX_ID") %>%
  mutate(total_trips = replace_na(total_trips, 0))

kable(head(hexagon_trips))
```

## 3. **Spatial Distribution Analysis**

In section 2.3, we derived an analytical hexagon data of 375m to represent the traffic analysis zone (TAZ). We also processed

Time period filtering for 3 time intervals will be filtered to compute passenger trips generated by origin at hexagon level. This is also done with deriving an analytical hexagon data to represent the traffic analysis.

Weekday morning peak \>\>6am - 9am (Bus Tap Time)

Weekday afternoon peak \>\> 5pm - 8pm (Bus Tap Time)

Weekend/holiday \>\> 11am - 8pm (Bus Tap Time)

### 3.1 Peak Hour Trips by HexID

::: panel-tabset
## Weekday AM

Filter the Bus tap on the time between 6am to 9am as morning peak hours during weekdays.

```{r}
AM_weekday_sort <- trips_sort %>%
  filter(HOUR_OF_DAY %in% c(6, 7, 8, 9),
         DAY_TYPE == "WEEKDAY") %>%
  group_by(HEX_ID) %>%
  summarise(AM_TRIPS = sum(TRIPS)) %>%
  arrange(desc(AM_TRIPS))
```

```{r}

am_plot <- ggplot(AM_weekday_sort, 
       aes(x = reorder(HEX_ID, -AM_TRIPS), y = AM_TRIPS)) +
  geom_col(fill = "lightblue", alpha = 0.8) +
  labs(title = "Weekday Morning Peak Hour Passenger Trips (6-9 AM)",
       subtitle = paste("Total", format(sum(AM_weekday_sort$AM_TRIPS), big.mark = ","), 
                       "trips across", nrow(AM_weekday_sort), "traffic zones"),
       x = "Hexagon ID", 
       y = "Passenger Trips") +
  theme_minimal() +
  theme(panel.background = element_rect(fill = "#D0DDD0"),
        axis.text.x = element_text(angle = 45, hjust = 1, size = 6),
        plot.title = element_text(face = "bold", size = 12),
        plot.subtitle = element_text(size = 9, color = "gray40")) +
  scale_y_continuous(labels = scales::comma)

# Display the plot
am_plot
```

## Weekday PM

Filter the Bus tap on the time between 5pm to 8pm as afternoon peak hours during weekdays.

```{r}
PM_weekday_sort <- trips_sort %>%
  filter(HOUR_OF_DAY %in% c(17, 18, 19, 20),
         DAY_TYPE == "WEEKDAY") %>%
  group_by(HEX_ID) %>%
  summarise(PM_TRIPS = sum(TRIPS)) %>%
  arrange(desc(PM_TRIPS))
```

```{r}
# PM Peak Visualization
pm_plot <- ggplot(PM_weekday_sort, 
       aes(x = reorder(HEX_ID, -PM_TRIPS), y = PM_TRIPS)) +
  geom_col(fill = "yellow", alpha = 0.8) +
  labs(title = "Weekday Afternoon Peak Hour Passenger Trips (5-8 PM)",
       subtitle = paste("Total", format(sum(PM_weekday_sort$PM_TRIPS), big.mark = ","), 
                       "trips across", nrow(PM_weekday_sort), "traffic zones"),
       x = "Hexagon ID", 
       y = "Passenger Trips") +
  theme_minimal() +
  theme(panel.background = element_rect(fill = "#D0DDD0"),
        axis.text.x = element_text(angle = 45, hjust = 1, size = 6),
        plot.title = element_text(face = "bold", size = 12),
        plot.subtitle = element_text(size = 9, color = "gray40")) +
  scale_y_continuous(labels = scales::comma)

# Display the plot
pm_plot
```

## Weekend/PH

Filter the Bus tap on the time between 11am to 8pm as morning peak hours during weekend or holidays.

```{r}
weekend_sort <- trips_sort %>%
  filter(HOUR_OF_DAY %in% c(11:20),
         DAY_TYPE == "WEEKENDS/HOLIDAY") %>%
  group_by(HEX_ID) %>%
  summarise(WKD_TRIPS = sum(TRIPS)) %>%
  arrange(desc(WKD_TRIPS))
```

```{r}
# Weekend/Holiday Visualization
weekend_plot <- ggplot(weekend_sort, 
       aes(x = reorder(HEX_ID, -WKD_TRIPS), y = WKD_TRIPS)) +
  geom_col(fill = "#A376A2", alpha = 0.8) +
  labs(title = "Weekend/Holiday Passenger Trips (11AM-8PM)",
       subtitle = paste("Total", format(sum(weekend_sort$WKD_TRIPS), big.mark = ","), 
                       "trips across", nrow(weekend_sort), "traffic zones"),
       x = "Hexagon ID", 
       y = "Passenger Trips") +  # CORRECTED TITLE
  theme_minimal() +
  theme(panel.background = element_rect(fill = "#D0DDD0"),
        axis.text.x = element_text(angle = 45, hjust = 1, size = 6),
        plot.title = element_text(face = "bold", size = 12),
        plot.subtitle = element_text(size = 9, color = "gray40")) +
  scale_y_continuous(labels = scales::comma)

# Display the plot
weekend_plot
```
:::

### 3.2 Comparative Urban Mobility Pattern

```{r}

AM_top10 <- AM_weekday_sort %>% slice_head(n = 10)

ggplot(AM_top10, 
       aes(x = reorder(HEX_ID, -AM_TRIPS), y = AM_TRIPS)) +
  geom_col(fill = "lightblue", alpha = 0.7) +
  labs(title = "Top 10 Hexagons: Weekday Morning Peak Trips (6-9 AM)",
       subtitle = "Major origin zones for morning commute",
       x = "Hexagon ID", 
       y = "Passenger Trips") +
  theme_minimal() +
  theme(axis.text.x = element_text(angle = 45, hjust = 1),
        plot.title = element_text(face = "bold"),
        panel.grid.major.x = element_blank()) +
  scale_y_continuous(labels = scales::comma) +
  geom_text(aes(label = scales::comma(AM_TRIPS)), 
            vjust = -0.5, size = 2.5, color = "black")
```

```{r}

max_trips <- max(c(max(weekend_sort$WKD_TRIPS), 
                   max(AM_weekday_sort$AM_TRIPS), 
                   max(PM_weekday_sort$PM_TRIPS)))



# Morning Commute Pattern
am_plot <- ggplot(AM_weekday_sort, 
       aes(x = reorder(HEX_ID, AM_TRIPS), y = AM_TRIPS)) +
  geom_col(fill = "lightblue", alpha = 0.85, width = 0.8) +
  labs(title = " MORNING COMMUTE\n(6-9AM Weekdays)",
       x = NULL, 
       y = "Passenger Trips") + 
  theme_minimal() +
  theme(
    panel.background = element_rect(fill = "#F0F8FF"),
    axis.text.y = element_blank(),  # Cleaner without hex IDs
    axis.text.x = element_text(size = 9, color = "black"),
    plot.title = element_text(size = 12, face = "bold", hjust = 0.5, color = "#4682B4"),
    axis.title.x = element_text(size = 10, face = "bold"),
    panel.grid.major.x = element_line(color = "gray80"),
    panel.grid.minor.x = element_blank(),
    panel.grid.major.y = element_blank(),
    plot.margin = margin(10, 10, 10, 10)
  ) +
  scale_y_continuous(limits = c(0, max_trips), 
                     labels = scales::comma,
                     breaks = scales::pretty_breaks(n = 6)) +
  coord_flip()

# Evening Commute Pattern  
pm_plot <- ggplot(PM_weekday_sort, 
       aes(x = reorder(HEX_ID, PM_TRIPS), y = PM_TRIPS)) +
  geom_col(fill = "yellow", alpha = 0.85, width = 0.8) +
  labs(title = "EVENING COMMUTE\n(5-8PM Weekdays)",
       x = NULL, 
       y = "Passenger Trips") + 
  theme_minimal() +
  theme(
    panel.background = element_rect(fill = "#F0F8FF"),
    axis.text.y = element_blank(),  # Cleaner without hex IDs
    axis.text.x = element_text(size = 9, color = "black"),
    plot.title = element_text(size = 12, face = "bold", hjust = 0.5, color = "black"),
    axis.title.x = element_text(size = 10, face = "bold"),
    panel.grid.major.x = element_line(color = "gray80"),
    panel.grid.minor.x = element_blank(),
    panel.grid.major.y = element_blank(),
    plot.margin = margin(10, 10, 10, 10)
  ) +
  scale_y_continuous(limits = c(0, max_trips), 
                     labels = scales::comma,
                     breaks = scales::pretty_breaks(n = 6)) +
  coord_flip()

# Weekend Pattern
weekend_plot <- ggplot(weekend_sort, 
       aes(x = reorder(HEX_ID, WKD_TRIPS), y = WKD_TRIPS)) +
  geom_col(fill = "#8B008B", alpha = 0.85, width = 0.8) +
  labs(title = "WEEKEND & HOLIDAY\n(11AM-8PM)",
       x = NULL, 
       y = "Passenger Trips") + 
  theme_minimal() +
  theme(
    panel.background = element_rect(fill = "#F0F8FF"),
    axis.text.y = element_blank(),  # Cleaner without hex IDs
    axis.text.x = element_text(size = 9, color = "black"),
    plot.title = element_text(size = 12, face = "bold", hjust = 0.5, color = "#8B008B"),
    axis.title.x = element_text(size = 10, face = "bold"),
    panel.grid.major.x = element_line(color = "gray80"),
    panel.grid.minor.x = element_blank(),
    panel.grid.major.y = element_blank(),
    plot.margin = margin(10, 10, 10, 10)
  ) +
  scale_y_continuous(limits = c(0, max_trips), 
                     labels = scales::comma,
                     breaks = scales::pretty_breaks(n = 6)) +
  coord_flip()

# COMBINE 
combined_plot <- am_plot + pm_plot + weekend_plot + 
  plot_layout(ncol = 3) +
  plot_annotation(
    title = "COMPARATIVE URBAN MOBILITY PATTERNS",
    subtitle = "Visualizing passenger trip volumes across different time periods in Singapore",
    caption = paste(
      "Data: 826 traffic analysis zones |",
      "Scale: 0 to", format(max_trips, big.mark = ","), "trips |",
      "Bars ordered by trip volume (high to low)"
    ),
    theme = theme(
      plot.title = element_text(face = "bold", size = 18, hjust = 0.5, margin = margin(b = 10)),
      plot.subtitle = element_text(size = 14, hjust = 0.5, color = "gray40", margin = margin(b = 15)),
      plot.caption = element_text(size = 10, color = "gray50", hjust = 0.5, margin = margin(t = 10)),
      plot.background = element_rect(fill = "white", color = NA)
    )
  )

# Display the final plot
combined_plot
```

The chart shows the distribution of passenger trip volumes during three time periods in Singapore: weekday morning (6–9 AM), weekday evening (5–8 PM), and weekends/holidays (11 AM–8 PM). In all three periods, the curve rises steeply at the beginning and then levels off, indicating that a small number of zones account for a large share of total trips, while the majority of zones have relatively low passenger volumes. The evening period has the steepest curve, showing the highest concentration of trips in fewer zones, followed by the morning period. The weekend curve is slightly less steep, indicating a wider spread of passenger trips across zones compared to weekdays. Overall, trip volumes are not evenly distributed, with clear differences between weekday peaks and weekends.

## 4. Spatial Autocorrelation Analysis

### 4.1 Global Measures of Spatial Autocorrelation

```{r}
#| code-fold: true
# AM weekdays sorted by Hex ID
AM_weekday <- trips %>%
  filter(HOUR_OF_DAY %in% c(6, 7, 8, 9),
         DAY_TYPE == "WEEKDAY") %>%
  group_by(HEX_ID) %>%
  summarise(AM_TRIPS = sum(TRIPS)) %>%
  arrange(as.character(HEX_ID))

# PM weekdays sorted by Hex ID
PM_weekday <- trips %>%
  filter(HOUR_OF_DAY %in% c(17, 18, 19, 20),
         DAY_TYPE == "WEEKDAY") %>%
  group_by(HEX_ID) %>%
  summarise(PM_TRIPS = sum(TRIPS)) %>%
  arrange(as.character(HEX_ID))

# Weekend sorted by Hex ID
weekend <- trips %>%
  filter(HOUR_OF_DAY %in% c(11:20),
         DAY_TYPE == "WEEKENDS/HOLIDAY") %>%
  group_by(HEX_ID) %>%
  summarise(WKD_TRIPS = sum(TRIPS)) %>%
  arrange(as.character(HEX_ID))
```

```{r}
# Join all three datasets
hexagon_all <- hexagon %>%
  left_join(AM_weekday, by = "HEX_ID") %>%
  left_join(PM_weekday, by = "HEX_ID") %>%
  left_join(weekend, by = "HEX_ID") %>%
  mutate(across(c(AM_TRIPS, PM_TRIPS, WKD_TRIPS), 
                ~replace_na(., 0))) # Handle any NA values - we fill in zero for hexagons with no trips

# Check if join worked
names(hexagon_all)  
head(hexagon_all$PM_TRIPS)  
head(hexagon_all$AM_TRIPS)  
head(hexagon_all$WKD_TRIPS)  
```

::: panel-tabset
## AM weekdays

```{r}
#| fig-width: 12
#| align: center
#| code-fold: true

#tmap_mode("view")
am_tm <- tm_shape(mpsz_cl) +
  tm_polygons(fill = "#FFF4EA", alpha = 0.3) +
  tm_text("SUBZONE_N", size = 0.5) +
tm_shape(hexagon_all) +
  tm_fill("AM_TRIPS", palette = "-Spectral", fill_alpha = 0.5) +
  tm_borders(col = "#B3C8CF", lwd = 0.1) +
  tm_layout(bg.color = "#D0DDD0",
            outer.margins = c(0.01, 0.01, 0.01, 0.01),
            main.title = "AM Peak Hour Trips by Hexagon",
            legend.position = c("right", "bottom"),
            frame = FALSE) +
    tm_view(set_zoom_limits = c(11,18))

# Add sqr-transformed column (not using)
hexagon_all <- hexagon_all %>%
  mutate(AM_TRIPS_SQRT = sqrt(AM_TRIPS))

#tmap_mode("view")
am_tm_qt <- tm_shape(mpsz_cl) +
  tm_polygons(fill = "#FFF4EA", fill_alpha = 0.3) +
  tm_text("SUBZONE_N", size = 0.5) +
  tm_shape(hexagon_all) +
  tm_fill("AM_TRIPS", palette = "-Spectral", fill_alpha = 0.6,
          style = "quantile") +
  tm_borders(col = "#B3C8CF", lwd = 0.1) +
  tm_layout(bg.color = "#D0DDD0",
            outer.margins = c(0.01, 0.01, 0.01, 0.01),
            main.title = "AM Peak Hour Trips (Quantile)",
            legend.position = c("right", "bottom"),
            frame = FALSE) +
  tm_shape(mrt_sf) +
  tm_symbols(   fill = "#CA8787", size = 0.5, col = "#D9E9CF"
  ) +
  tm_view(set_zoom_limits = c(11,18)) 

tmap_arrange(am_tm, am_tm_qt, 
             asp=1, ncol=1,
             sync = TRUE)
tmap_mode("plot")
```

## PM weekdays

```{r}
#| fig-width: 12
#| fig-height: 7
#| align: center
#| code-fold: true

#tmap_mode("view")
pm_tm <- tm_shape(mpsz_cl) +
  tm_polygons(fill = "#FFF4EA", alpha = 0.3) +
  tm_text("SUBZONE_N", size = 0.5) +
tm_shape(hexagon_all) +
  tm_fill("PM_TRIPS", palette = "-Spectral", fill_alpha = 0.5) +
  tm_borders(col = "#B3C8CF", lwd = 0.1) +
  tm_layout(bg.color = "#D0DDD0",
            outer.margins = c(0.01, 0.01, 0.01, 0.01),
            main.title = "PM Peak Hour Trips by Hexagon",
            legend.position = c("right", "bottom"),
            frame = FALSE) +
  tm_view(set_zoom_limits = c(11,18))

# Add sqr-transformed column
hexagon_all <- hexagon_all %>%
  mutate(PM_TRIPS_SQRT = sqrt(PM_TRIPS))

#tmap_mode("view")

pm_tm_qt <- tm_shape(mpsz_cl) +
  tm_polygons(fill = "#FFF4EA", alpha = 0.3) +
  tm_text("SUBZONE_N", size = 0.5) +
  tm_shape(hexagon_all) +
  tm_fill("PM_TRIPS", palette = "-Spectral", fill_alpha = 0.6,
          style = "quantile"
          ) +
  tm_borders(col = "#B3C8CF", lwd = 0.1) +
  tm_layout(bg.color = "#D0DDD0",
            outer.margins = c(0.01, 0.01, 0.01, 0.01),
            main.title = "PM Peak Hour Trips (Quantile)",
            legend.position = c("right", "bottom"),
            frame = FALSE) +
  tm_shape(mrt_sf) +
  tm_symbols(fill = "#CA8787", size = 0.5, col = "#D9E9CF") +
  tm_view(set_zoom_limits = c(11,18))

tmap_arrange(pm_tm, pm_tm_qt, 
             asp=1, ncol=1,
             sync = TRUE)

tmap_mode("plot")
```

## Weekends

```{r}
#| fig-width: 12
#| fig-height: 7
#| align: center
#| code-fold: true

#tmap_mode("view")
wkd_tm <- tm_shape(mpsz_cl) +
  tm_polygons(fill = "#A1C2BD", alpha = 0.3) +
  tm_text("SUBZONE_N", size = 0.5) +
tm_shape(hexagon_all) +
  tm_fill("WKD_TRIPS", palette = "-Spectral", fill_alpha = 0.5) +
  tm_borders(col = "#B3C8CF", lwd = 0.1) +
  tm_layout(bg.color = "#D0DDD0",
            outer.margins = c(0.01, 0.01, 0.01, 0.01),
            main.title = "Weekend Peak Hour Trips by Hexagon",
            legend.position = c("right", "bottom"),
            frame = FALSE) +
    tm_view(set_zoom_limits = c(11,18))

# Add sqr-transformed column
hexagon_all <- hexagon_all %>%
  mutate(WKD_TRIPS_SQRT = sqrt(WKD_TRIPS))

#tmap_mode("view")
wkd_tm_qt <- tm_shape(mpsz_cl) +
  tm_polygons(fill = "#A1C2BD", alpha = 0.3) +
  tm_text("SUBZONE_N", size = 0.5) +
  tm_shape(hexagon_all) +
  tm_fill("WKD_TRIPS", palette = "-Spectral", fill_alpha = 0.6,
          style = "quantile") +
  tm_borders(col = "#B3C8CF", lwd = 0.1) +
  tm_layout(bg.color = "#D0DDD0",
            outer.margins = c(0.01, 0.01, 0.01, 0.01),
            main.title = "Weekend Peak Hour Trips (Quantile)",
            legend.position = c("right", "bottom"),
            frame = FALSE) +
  tm_shape(mrt_sf) +
  tm_symbols(fill = "#CA8787", size = 0.5, col = "#D9E9CF"
  ) +
  tm_view(set_zoom_limits = c(11,18))

tmap_arrange(wkd_tm, wkd_tm_qt, 
             asp=1, ncol=1,
             sync = TRUE)
tmap_mode("plot")
```
:::

This is for preparing the passenger trips data per hexagon area in `hexagon_all`.

#### **4.1.1 Compute (QUEEN) contiguity spatial weights**

This study examines whether passenger trips are evenly spread across all hexagons. Earlier charts already suggest that trips are unevenly distributed, and this section aims to confirm that using statistical evidence. Once unevenness is established, the next step is to determine whether there are specific clusters of high or low activity. If such clusters are found, the final step is to identify their exact locations.

```{r}
wm_q <- poly2nb(hexagon_all, queen = TRUE)
summary(wm_q)
```

The summary report above shows that there are 826 regions (hexagons) in the Singapore. The most connected area unit has 6 neighbours. 1 region has 0 neighbours (completely isolated).

The neighbour structure forms 6 separate connected subgraphs, meaning the entire study area is not fully connected. This can occur when some hexagons are isolated (e.g., near borders or water bodies).

```{r}
longitude <- map_dbl(hexagon_all$geometry, ~st_centroid(.x)[[1]])
```

```{r}
latitude <- map_dbl(hexagon_all$geometry, ~st_centroid(.x)[[2]])
```

```{r}
coords <- cbind(longitude, latitude)
head(coords)
```

```{r}
plot(hexagon_all$geometry, border="lightgrey")
plot(wm_q, coords, pch = 19, cex = 0.6, add = TRUE, col= "red")
```

#### **4.1.2 Row-standardised weights matrix**

Now we want to assign weights t =o each neighboring hexagon. In this study, each hexagon will be assigned equal weight (stylep = W).

```{r}
rswm_q <- nb2listw(wm_q,
                   style = "W", zero.policy = TRUE)
rswm_q
```

#### **4.1.3 Moran's I Test**

Moran's I statistics testing is being performed for the 3 different periods of time by using `moran.test()` from spdep package.

::: panel-tabset
## Weekday AM

**Moran's I Test**

```{r}
moran.test(hexagon_all$AM_TRIPS,
           listw = rswm_q,
           zero.policy = TRUE, na.action = na.omit)
```

**Insight**:

The Moran’s I statistic for weekday morning bus trips is 0.34, with an expected value near zero (–0.0012) and a p-value \< 2.2 × 10⁻¹⁶. This indicates that the observed value is much higher than expected under randomness. The large standard deviate (14.513) also supports this.

This means there is strong and statistically significant positive spatial autocorrelation in weekday morning passenger trips. In simpler terms, areas with high passenger volumes are located close to other areas with high volumes, and areas with low passenger volumes tend to cluster together as well.

This confirms that bus trip activity during morning peak hours is not randomly distributed across space but shows clear spatial clustering.

**Monte Carlo Moran's I** :simulation for 1,000 times.

```{r}
set.seed(1234)
bperm = moran.mc(hexagon_all$AM_TRIPS,
                 listw = rswm_q,
                 nsim = 999,
                 zero.policy = TRUE, na.action = na.omit)
bperm
```

The Monte Carlo simulation was run 1,000 times (999 simulations + 1 observed) to test the significance of the Moran’s I statistic more robustly. The observed Moran’s I is 0.34, which ranked 1,000 out of 1,000, meaning none of the simulated random distributions produced a value higher than the observed one.

The p-value of 0.001 indicates that the probability of observing such a strong Moran’s I by random chance is very low (0.1%). This provides strong statistical evidence that the spatial clustering pattern detected earlier is not due to random variation, but reflects a real spatial structure in the weekday morning bus trip data.

**Visualise Moran's I Test**

We plot the distribution of statistical values below.

```{r}
mean <- mean(bperm$res[1:999])
print(paste0("Mean of permutations: ", mean))

var <- var(bperm$res[1:999])
print(paste0("Variance: ", var))

summary(bperm$res[1:999])
```

```{r}
perm_data <- data.frame(moran_i = bperm$res)

ggplot(perm_data, aes(x = moran_i)) +
  geom_histogram(bins = 20, fill = "lightblue", color = "#DCDCDC") +
  geom_vline(xintercept = 0, color = "blue", linewidth = 0.5) +
  geom_vline(xintercept = 0.34, color = "red", linewidth = 0.5) +
  labs(x = "Simulated Moran's I (Weekday AM)", y = "Frequency") +
  theme(plot.background = element_rect("#D0DDD0"),
        panel.background = element_rect(fill = "#D0DDD0"))
```

The red line on the far right indicates the actual observed Moran’s I for weekday morning bus trips (≈ 0.34). This observed value lies far outside the bulk of the simulated distribution, showing that the observed spatial pattern is much stronger than what would occur by chance.

This visual evidence supports the earlier **Moran’s I test** and **Monte Carlo p-value**, confirming the presence of **significant positive spatial autocorrelation** in passenger trip volumes during the morning peak period.

## Weekday PM

**Moran's I Test**

```{r}
moran.test(hexagon_all$PM_TRIPS,
           listw = rswm_q,
           zero.policy = TRUE, na.action = na.omit)
```

**Insight**

For weekday evening trips, the Moran’s I statistic is 0.108, with an expected value close to zero (–0.0012). The p-value is 9.748 × 10⁻⁷, which is far below 0.05, indicating statistical significance. The standard deviate is 4.7586, confirming that the observed value is much higher than what would be expected by random chance.

Although the Moran’s I value for PM trips is **positive and significant**, it is **lower than the AM value (0.34)**, meaning that **spatial clustering is present but weaker** in the evening compared to the morning. Passenger trips during weekday evenings are still not randomly distributed, but the spatial pattern is **less tightly clustered**.

One likely reason for this weaker clustering is the greater variety in trip origins during the evening. Unlike the morning, when most trips are concentrated in residential areas heading toward key destinations, evening trips tend to start from a wider mix of locations — such as workplaces, schools, commercial districts, and shopping areas — resulting in a more scattered spatial pattern.

**Monte Carlo Moran's I**

```{r}
set.seed(1234)
bperm_pm = moran.mc(hexagon_all$PM_TRIPS,
                 listw = rswm_q,
                 nsim = 999,
                 zero.policy = TRUE, na.action = na.omit)
bperm_pm
```

The p-value of 0.001 provides strong evidence to reject the null hypothesis, confirming that **weekday PM passenger trips exhibit significant spatial clustering.** This Monte Carlo result supports and reinforces the analytical Moran’s I test, showing that the clustering pattern is unlikely to have occurred by random chance.

**Visualise Moran's I Test**

```{r}
mean_pm <- mean(bperm_pm$res[1:999])
print(paste0("Mean of permutations: ", mean))

var_pm <- var(bperm_pm$res[1:999])
print(paste0("Variance: ", var))

summary(bperm_pm$res[1:999])
```

```{r}
perm_data_pm <- data.frame(moran_i = bperm_pm$res)

ggplot(perm_data_pm, aes(x = moran_i)) +
  geom_histogram(bins = 20, fill = "yellow", color = "#DCDCDC") +
  geom_vline(xintercept = 0, color = "blue", linewidth = 0.5) +
  geom_vline(xintercept = 0.10792, color = "red", linewidth = 0.5) +
  labs(x = "Simulated Moran's I (Weekday PM)", y = "Frequency") +
  theme(plot.background = element_rect("#D0DDD0"),
        panel.background = element_rect(fill = "#D0DDD0"))
```

The red line on the far right represents the observed Moran’s I for weekday PM bus trips (≈ 0.108). Although lower than the AM peak value, this observed value still lies well outside the bulk of the simulated distribution, which is centered around zero (blue line). This indicates that the observed spatial pattern is stronger than what would be expected by chance.

This visual evidence **supports the PM Moran’s I test and Monte Carlo p-value (0.001)**, confirming that **significant positive spatial autocorrelation** exists during the evening peak period. However, the clustering effect is **weaker than in the morning**, reflecting a more dispersed spatial distribution of passenger trips.

## Weekend/Public holiday

**Moran's I Test**

```{r}
moran.test(hexagon_all$WKD_TRIPS,
           listw = rswm_q,
           zero.policy = TRUE, na.action = na.omit)
```

**Insight**

The Moran’s I statistic for weekend and public holiday trips is 0.235, with an expected value close to zero (–0.0012). The standard deviate of 10.078 and p-value \< 2.2 × 10⁻¹⁶ indicate that the observed value is far above what would be expected by random chance.

This provides **very strong statistical evidence** to reject the null hypothesis, confirming that **spatial clustering is present during weekends and public holidays**. The Moran’s I value is **lower than AM peak (0.34)** but **higher than PM peak (0.108)**, meaning the **weekend clustering is moderate**—more pronounced than in the evening but less concentrated than morning commutes.

This suggests that **weekend travel patterns are still spatially clustered**, likely reflecting popular leisure, shopping, and recreation areas, but with a more even distribution across the city compared to workday peaks.

**Monte Carlo Moran's I**

```{r}
set.seed(1234)
bperm_wkd = moran.mc(hexagon_all$WKD_TRIPS,
                 listw = rswm_q,
                 nsim = 999,
                 zero.policy = TRUE, na.action = na.omit)
bperm_wkd
```

The p-value of 0.001 provides strong evidence to reject the null hypothesis, confirming that **weekend and public holiday passenger trips exhibit significant spatial clustering**. This Monte Carlo result supports and reinforces the analytical Moran’s I test, indicating that the observed clustering pattern is highly unlikely to have occurred by random chance.

**Visualise Moran's I Test**

```{r}
mean_wkd <- mean(bperm_wkd$res[1:999])
print(paste0("Mean of permutations: ", mean))

var_wkd <- var(bperm_wkd$res[1:999])
print(paste0("Variance: ", var))

summary(bperm_wkd$res[1:999])
```

```{r}
perm_data_wkd <- data.frame(moran_i = bperm_wkd$res)

ggplot(perm_data_wkd, aes(x = moran_i)) +
  geom_histogram(bins = 20, fill = "#A376A2", color = "#DCDCDC") +
  geom_vline(xintercept = 0, color = "blue", linewidth = 0.5) +
  geom_vline(xintercept = 0.2349, color = "red", linewidth = 0.5) +
  labs(x = "Simulated Moran's I (Weekend)", y = "Frequency") +
  theme(plot.background = element_rect(fill = "#D0DDD0"),
        panel.background = element_rect(fill = "#D0DDD0"))
```

The histogram of simulated Moran’s I values for weekend/public holiday trips shows that most of the simulated values cluster around zero (blue line), representing the expected result under random spatial distribution. The observed Moran’s I value of 0.235 (red line) lies far to the right of the simulated distribution, clearly outside the main peak.
:::

|  |  |  |  |  |
|---------------|---------------|---------------|---------------|---------------|
| **Time Period** | **Moran's I** | **p-value** | **Clustering Strength** | **Pattern Description** |
| Weekday AM (6–9 AM) | 0.34 | \< 2.2e-16 | Strong | Concentrated commute flows from residential to key nodes |
| Weekday PM (5–8 PM) | 0.10792 | 0.001 | Weak | More dispersed origins, staggered travel times |
| Weekend/Public Holiday | 0.2349 | 0.001 | Moderate | Leisure-oriented flows, spread across multiple zones |

The highest clustering occurs during weekday morning peak, driven by concentrated commuting flows. The weakest clustering is during weekday evenings, when trip origins are more scattered and travel behaviour is less synchronized. Weekends and public holidays show a moderate level of clustering, indicating strong but more spatially distributed travel demand.

Clustering strength: **AM \> Weekend \> PM**.

## 5. Local Spatial Autocorrelation (LISA)

The Global measures of spatial autocorrelation suggests that all three study periods have clustering spatial patterns. We will now use Local Measures of Spatial Autocorrelation to compute the statistic of the passenger trips, and understand where the significant clusters are located by displaying the LMSA maps.

Below we will continue to use spdep to compute Local Moran's I statistic.

### 5.2.1 Computing Contiguity Spatial Weights

```{r}
hexagon_clean <- hexagon_all %>%
  mutate(nb = st_contiguity(geometry),
         wt = st_weights(nb, style = "W",
                         , allow_zero = TRUE),
         .before = 1) 

```

```{r}
summary(hexagon_clean$nb)
```

Based on the summary report, we understand that there is one hexagon area, HexID = 826, without any neighbors. We will filter this neighbour out to process futher LMSA statistic and analysis as it cannot create any weights.

### 5.2.2 Computing Local Moran's I

``` r
install.packages("remotes")
remotes::install_github("JosiahParry/sfdep")
```

```{r}
# Remove by HEX_ID
hexagon_clean_ft <- hexagon_all %>%
  filter(HEX_ID != "H0826") 
```

```{r}
# Recreate nb and wt
hexagon_clean_ft <- hexagon_clean_ft %>%
  mutate(
    nb = st_contiguity(geometry),
    wt = st_weights(nb, style = "W")
  )
```

::: panel-tabset
## Weekday AM

```{r}
#| eval: false
set.seed(1234)
lisa_am <- hexagon_clean_ft %>%
  mutate(local_moran = local_moran(
    AM_TRIPS, nb, wt, nsim = 99
  ), .before = 1) %>%
  unnest(local_moran)

write_rds(lisa_am, "data/rds/lisa_am.rds")
```

```{r}
lisa_am <- read_rds("data/rds/lisa_am.rds")
```

```{r}
glimpse(lisa_am)
```

```{r}
#| fig-width: 12
# AM Local Moran's I
lmsa_am <- tm_shape(mpsz_cl) +
  tm_polygons(fill = "#FFF4EA", fill_alpha = 0.3) +
#  tm_text("SUBZONE_N", size = 0.5) +
tm_shape(lisa_am) +
  tm_polygons(fill = "ii",
              fill.scale = tm_scale_intervals(
                style = "pretty",
                n = 5,
                values = "-brewer.RdBu"), 
                fill_alpha = 0.7,
              fill.legend = tm_legend(
                title = "Local Moran's I")) + 
  tm_borders(fill_alpha = 0.5) +
  tm_title("LISA Map - Local Moran's I AM Weekday Trips") +
  tm_layout(
    legend.position = c("right", "bottom"),
    bg.color = "#D0DDD0")

#Local Moran's I p-value
lmsa_am_p <- tm_shape(mpsz_cl) +
  tm_polygons(fill = "#FFF4EA", fill_alpha = 0.3) +
tm_shape(lisa_am) +
  tm_polygons(fill = "p_ii", 
              fill.scale = tm_scale_intervals(
                breaks = c(-Inf, 0.001, 0.01, 0.05, 0.1, Inf),
                values = "-brewer.Reds"),
              fill.legend = tm_legend(
                title = "p-value")) + 
  tm_borders(fill_alpha = 0.5) +
  tm_title("LISA Map - p-values of Local Moran's I AM Weekday Trips") +
  tm_layout(legend.position = c("right", "bottom"),
            bg.color = "#D0DDD0")
lmsa_am
lmsa_am_p
```

## Weekday PM

```{r}
#| eval: false
set.seed(1234)
lisa_pm <- hexagon_clean_ft %>%
  mutate(local_moran = local_moran(
    PM_TRIPS, nb, wt, nsim = 99
  ), .before = 1) %>%
  unnest(local_moran)

write_rds(lisa_pm, "data/rds/lisa_pm.rds")
```

```{r}
lisa_pm <- read_rds("data/rds/lisa_pm.rds")
```

```{r}
glimpse(lisa_pm)
```

```{r}
#| fig-width: 12
# PM Local Moran's I
lmsa_pm <- tm_shape(mpsz_cl) +
  tm_polygons(fill = "#FFF4EA", fill_alpha = 0.3) +
#  tm_text("SUBZONE_N", size = 0.5) +
tm_shape(lisa_pm) +
  tm_polygons(fill = "ii",
              fill.scale = tm_scale_intervals(
                style = "pretty",
                n = 5,
                values = "-brewer.RdBu"), 
                fill_alpha = 0.7,
              fill.legend = tm_legend(
                title = "Local Moran's I")) + 
  tm_borders(fill_alpha = 0.5) +
  tm_title("LISA Map - Local Moran's I PM Weekday Trips") +
  tm_layout(
    legend.position = c("right", "bottom"),
    bg.color = "#D0DDD0")

#Local Moran's I p-value
lmsa_pm_p <- tm_shape(mpsz_cl) +
  tm_polygons(fill = "#FFF4EA", fill_alpha = 0.3) +
tm_shape(lisa_pm) +
  tm_polygons(fill = "p_ii", 
              fill.scale = tm_scale_intervals(
                breaks = c(-Inf, 0.001, 0.01, 0.05, 0.1, Inf),
                values = "-brewer.Reds"),
              fill.legend = tm_legend(
                title = "p-value")) + 
  tm_borders(fill_alpha = 0.5) +
  tm_title("LISA Map - p-values of Local Moran's I PM Weeday Trips") +
  tm_layout(legend.position = c("right", "bottom"),
            bg.color = "#D0DDD0")
lmsa_pm
lmsa_pm_p
```

## Weedkend/PH

```{r}
#| eval: false
set.seed(1234)
lisa_wkd <- hexagon_clean_ft %>%
  mutate(local_moran = local_moran(
    WKD_TRIPS, nb, wt, nsim = 99
  ), .before = 1) %>%
  unnest(local_moran)

write_rds(lisa_wkd, "data/rds/lisa_wkd.rds")
```

```{r}
lisa_wkd <- read_rds("data/rds/lisa_wkd.rds")
```

```{r}
glimpse(lisa_wkd)
```

```{r}
#| fig-width: 12
# Weekend Local Moran's I
lmsa_wkd <- tm_shape(mpsz_cl) +
  tm_polygons(fill = "#FFF4EA", fill_alpha = 0.3) +
#  tm_text("SUBZONE_N", size = 0.5) +
tm_shape(lisa_wkd) +
  tm_polygons(fill = "ii",
              fill.scale = tm_scale_intervals(
                style = "pretty",
                n = 5,
                values = "-brewer.RdBu"), 
                fill_alpha = 0.7,
              fill.legend = tm_legend(
                title = "Local Moran's I")) + 
  tm_borders(fill_alpha = 0.5) +
  tm_title("LISA Map - Local Moran's I Weekend Trips") +
  tm_layout(
    legend.position = c("right", "bottom"),
    bg.color = "#D0DDD0")

#Local Moran's I p-value
lmsa_wkd_p <- tm_shape(mpsz_cl) +
  tm_polygons(fill = "#FFF4EA", fill_alpha = 0.3) +
tm_shape(lisa_wkd) +
  tm_polygons(fill = "p_ii", 
              fill.scale = tm_scale_intervals(
                breaks = c(-Inf, 0.001, 0.01, 0.05, 0.1, Inf),
                values = "-brewer.Reds"),
              fill.legend = tm_legend(
                title = "p-value")) + 
  tm_borders(fill_alpha = 0.5) +
  tm_title("LISA Map - p-values of Local Moran's I Weekend Trips") +
  tm_layout(legend.position = c("right", "bottom"),
            bg.color = "#D0DDD0")
lmsa_wkd
lmsa_wkd_p

```
:::

| Time Period | Spatial Pattern | Cluster Strength | Key Hotspot Characteristics |
|------------------|------------------|------------------|------------------|
| **Weekday AM** | Highly clustered | Strong | Residential to employment corridors; strong central & regional nodes |
| **Weekday PM** | Moderately dispersed | Weak | Fewer concentrated hotspots; scattered evening activity |
| **Weekend/Public Holiday** | Re-emerging clusters | Moderate | Leisure, shopping, and recreational zones become dominant |

### 5.3 Hot Spot/ Cold Spot Analysis

Hot spots and cold spots analysis is a technique used in spatial statistics to identify statistically significant clusters of high values (hot spots) and low values (cold spots) of a geographically referenced attribute . These analyses help visualize and understand spatial patterns, revealing areas where data points are clustered more densely than would be expected by random chance.

```{r}
ct <- critical_threshold(st_geometry(hexagon_all))
ct
```

The summary report shows that the largest first nearest neighbour distance is 2km, so using this as the upper threshold gives certainty that all units will have at least one neighbour.

#### Fixed Distance Weights

```{r}
fdw <- hexagon_all %>% 
  mutate(nb = include_self(
    st_dist_band(
      st_geometry(geometry), 
                 upper = ct)),
    wt = st_weights(nb, 
                      style = "W"),
    .before = 1) 
```

#### Adaptive Distance Weights

```{r}
adw <- hexagon_all %>%
  mutate(nb = include_self(
    st_knn(
      st_geometry(geometry),
      k = 6)),
    wt = st_weights(
      nb, style = "W"),
    .before = 1)
```

#### Computing Local Gi\* and Visualising

Local Gi\* (pronounced “G-i-star”) is a Getis-Ord statistic used in spatial analysis to identify statistically significant hot spots and cold spots of high or low attribute values within a dataset. 

#### Local Gi\* of AM Weekday

```{r}
HCSA_AM_fdw <- fdw %>%
  mutate(
    gistar = local_gstar_perm(
      AM_TRIPS, nb, wt, nsim = 99),
    .before = 1) %>%
  unnest(gistar)
```

```{r}
Gi_star_map <-  tm_shape(HCSA_AM_fdw) +
  tm_polygons(fill = "gi_star",
              fill.scale = tm_scale_intervals(
                style = "pretty",
                n = 5,
                values = "-brewer.rd_bu"),
              fill.legend = tm_legend(
                title = "local Gi*",
                position = tm_pos_in(
                  "left", "bottom"))) + 
  tm_borders(fill_alpha = 0.5) +
  tm_title("Local Gi* of AM Weekday Trips")

p_values_map <- tm_shape(HCSA_AM_fdw) +
  tm_polygons(fill = "p_sim", 
              fill.scale = tm_scale_intervals(
                breaks = c(0, 0.001, 0.01, 0.05, 0.1, 1),
                values = "-brewer.reds"),
              fill.legend = tm_legend(
                title = "p-value",
      position = tm_pos_in("left", "bottom")
    )) + 
  tm_borders(fill_alpha = 0.5) +
  tm_title("p-values of local Gi* of AM Weekday Trips (fixed distance)")

tmap_arrange(Gi_star_map, p_values_map, asp=1, ncol=2)
```

#### Local Gi\* of PM Weekday

```{r}
#| echo: false 
 HCSA_PM_fdw <- fdw %>%
  mutate(
    gistar = local_gstar_perm(
      PM_TRIPS, nb, wt, nsim = 99),
    .before = 1) %>%
  unnest(gistar)
```

```{r}
#| echo: false 
Gi_star_map <- tm_shape(HCSA_PM_fdw) +
  tm_polygons(fill = "gi_star",
              fill.scale = tm_scale_intervals(
                style = "pretty",
                n = 5,
                values = "-brewer.rd_bu"),
              fill.legend = tm_legend(
                title = "local Gi*",
                position = tm_pos_in(
                  "left", "bottom"))) + 
  tm_borders(fill_alpha = 0.5) +
  tm_title("Local Gi* of PM Weekday Trips")

p_values_map <- tm_shape(HCSA_PM_fdw) +
  tm_polygons(fill = "p_sim", 
              fill.scale = tm_scale_intervals(
                breaks = c(0, 0.001, 0.01, 0.05, 0.1, 1),
                values = "-brewer.reds"),
              fill.legend = tm_legend(
                title = "p-value",
      position = tm_pos_in("left", "bottom")
    )) + 
  tm_borders(fill_alpha = 0.5) +
  tm_title("p-values of local Gi* of PM Weekday Trips (fixed distance)")

tmap_arrange(Gi_star_map, p_values_map, asp=1, ncol=2)
```

#### Local Gi\* of Weekend

```{r}
 #| echo: false 
 HCSA_WKD_fdw <- fdw %>%
  mutate(
    gistar = local_gstar_perm(
      WKD_TRIPS, nb, wt, nsim = 99),
    .before = 1) %>%
  unnest(gistar)
```

```{r}
#| echo: false 
Gi_star_map <- tm_shape(HCSA_WKD_fdw) +
  tm_polygons(fill = "gi_star",
              fill.scale = tm_scale_intervals(
                style = "pretty",
                n = 5,
                values = "-brewer.rd_bu"),
              fill.legend = tm_legend(
                title = "local Gi*",
                position = tm_pos_in(
                  "left", "bottom"))) + 
  tm_borders(fill_alpha = 0.5) +
  tm_title("Local Gi* of Weekend Trips")

p_values_map <- tm_shape(HCSA_WKD_fdw) +
  tm_polygons(fill = "p_sim", 
              fill.scale = tm_scale_intervals(
                breaks = c(0, 0.001, 0.01, 0.05, 0.1, 1),
                values = "-brewer.reds"),
              fill.legend = tm_legend(
                title = "p-value",
      position = tm_pos_in("left", "bottom")
    )) + 
  tm_borders(fill_alpha = 0.5) +
  tm_title("p-values of local Gi* of Weekend Trips (fixed distance)")

tmap_arrange(Gi_star_map, p_values_map, asp=1, ncol=2)
```

### 6. HCSA Map Plotting

#### HCSA of AM Weekday

```{r}
HCSA_AM_fdw <- HCSA_AM_fdw %>%
  mutate(HCSA_cluster = case_when(
    p_sim > 0.05 ~ "Insignificant",
    p_sim <= 0.05 & cluster == "High" ~ "Hot spot",
    p_sim <= 0.05 & cluster == "Low"  ~ "Cold spot",
    TRUE ~ "Other"),
    HCSA_cluster = factor(
      HCSA_cluster,
      levels = c("Insignificant", "Hot spot", "Cold spot")
    ),
    .before = 1
  )
```

```{r}
HCSA_AM_map <- tm_shape(HCSA_AM_fdw) + 
  tm_polygons(
    fill = "HCSA_cluster",
    fill.scale = tm_scale_categorical(
      values = c(
        "grey80",      # Insignificant
        "red",        # Low-Low
        "blue"          # High-High
      )
    ),
    fill.legend = tm_legend(
      title = "HSCA Cluster",
      position = tm_pos_in("left", "bottom"))
  ) +
  tm_borders() +
  tm_title("HCSA Clusters of AM Weekday (p < 0.05)")
HCSA_AM_map
```

#### HCSA of PM Weekday

```{r}
#| echo: false 
HCSA_PM_fdw <- HCSA_PM_fdw %>%
  mutate(HCSA_cluster = case_when(
    p_sim > 0.05 ~ "Insignificant",
    p_sim <= 0.05 & cluster == "High" ~ "Hot spot",
    p_sim <= 0.05 & cluster == "Low"  ~ "Cold spot",
    TRUE ~ "Other"),
    HCSA_cluster = factor(
      HCSA_cluster,
      levels = c("Insignificant", "Hot spot", "Cold spot")
    ),
    .before = 1
  )
```

```{r}
#| echo: false 
HCSA_PM_map <- tm_shape(HCSA_PM_fdw) + 
  tm_polygons(
    fill = "HCSA_cluster",
    fill.scale = tm_scale_categorical(
      values = c(
        "grey80",      # Insignificant
        "red",        # Low-Low
        "blue"          # High-High
      )
    ),
    fill.legend = tm_legend(
      title = "HSCA Cluster",
      position = tm_pos_in("left", "bottom"))
  ) +
  tm_borders() +
  tm_title("HCSA Clusters of PM Weekday (p < 0.05)")
HCSA_PM_map
```

#### HCSA of Weekend

```{r}
#| echo: false 
HCSA_WKD_fdw <- HCSA_WKD_fdw %>%
  mutate(HCSA_cluster = case_when(
    p_sim > 0.05 ~ "Insignificant",
    p_sim <= 0.05 & cluster == "High" ~ "Hot spot",
    p_sim <= 0.05 & cluster == "Low"  ~ "Cold spot",
    TRUE ~ "Other"),
    HCSA_cluster = factor(
      HCSA_cluster,
      levels = c("Insignificant", "Hot spot", "Cold spot")
    ),
    .before = 1
  )
```

```{r}
#| echo: false 
HCSA_WKD_map <- tm_shape(HCSA_WKD_fdw) + 
  tm_polygons(
    fill = "HCSA_cluster",
    fill.scale = tm_scale_categorical(
      values = c(
        "grey80",      # Insignificant
        "red",        # Low-Low
        "blue"          # High-High
      )
    ),
    fill.legend = tm_legend(
      title = "HSCA Cluster",
      position = tm_pos_in("left", "bottom"))
  ) +
  tm_borders() +
  tm_title("HCSA Clusters of Weekend (p < 0.05)")
HCSA_WKD_map
```

#### Hot Spot and Cold Spot Analysis (HCSA) Summary

-   **Weekday AM (6–9 AM)**\
    Hot spots are strongly concentrated in Orchard–Novena, Woodlands–Yishun, Tampines–Bedok, and Jurong East, reflecting intense commuter flows from residential to employment areas. Cold spots occur mainly along Tuas, northern fringes, and green buffer zones, indicating low activity.

-   **Weekday PM (5–8 PM)**\
    Hot spots become fewer and more scattered, with smaller clusters around Tampines, Woodlands, and western interchanges. Cold spots expand across the West and North, showing dispersed evening travel and less concentrated demand.

-   **Weekend / Public Holiday (11 AM–8 PM)**\
    Hot spots reappear at Downtown Core, Tampines, HarbourFront–Sentosa, and Jurong East, driven by leisure and retail trips. Cold spots remain stable in industrial and low-density zones.

-   Overall, AM peaks show the **strongest clustering** (commuting), PM peaks are **more dispersed**, and weekends reflect **leisure-oriented movement**.

#### **Implication for Transport Planning:**

-   **AM peak** requires targeted capacity allocation on routes serving **residential → employment corridors**

-   **PM peak** may benefit from **more flexible or demand-responsive services**.

-   **Weekend planning** should emphasize connectivity to **leisure and retail destinations** rather than traditional commute hubs.

-   Persistent **cold spots** can be used to optimize route coverage or evaluate the feasibility of alternative services.

## 7. Emerging Hot Spot Analysis (EHSA)

Emerging Hot Spots Analysis (EHSA) is a spatio-temporal statistical technique that identifies and categorizes areas where clusters of high or low values of a phenomenon are developing, intensifying, or diminishing over time. 

```{r}
# Check the structure of your trips data
glimpse(trips)

# Let's create proper month numbering if it doesn't exist
# Since you have August and September 2025 data
trips_with_month <- trips %>%
  mutate(
    MONTH_NUM = case_when(
      MONTH == "2025-08" ~ 1,
      MONTH == "2025-09" ~ 2,
      TRUE ~ NA_real_
    )
  ) %>%
  filter(!is.na(MONTH_NUM))

# Verify the month numbering
table(trips_with_month$MONTH, trips_with_month$MONTH_NUM)
```

```{r}

```

#### 7.1 Create Space-Time Cubes

```{r}
# Create monthly aggregated data for EHSA
monthly_trips <- trips_with_month %>%
  group_by(MONTH, MONTH_NUM, HEX_ID, DAY_TYPE) %>%
  summarise(
    AM_TRIPS = sum(TRIPS[HOUR_OF_DAY %in% c(6,7,8,9) & DAY_TYPE == "WEEKDAY"], na.rm = TRUE),
    PM_TRIPS = sum(TRIPS[HOUR_OF_DAY %in% c(17,18,19,20) & DAY_TYPE == "WEEKDAY"], na.rm = TRUE),
    WKD_TRIPS = sum(TRIPS[HOUR_OF_DAY %in% c(11:20) & DAY_TYPE == "WEEKENDS/HOLIDAY"], na.rm = TRUE),
    .groups = 'drop'
  )

# Create space-time cubes for each time period
create_spacetime_cube <- function(time_period) {
  cube_data <- monthly_trips %>%
    select(MONTH, MONTH_NUM, HEX_ID, !!sym(time_period)) %>%
    rename(trips = !!sym(time_period)) %>%
    filter(trips > 0) %>%  # Remove zeros for better analysis
    arrange(HEX_ID, MONTH_NUM)
  
  # Join with hexagon geometry
  cube_sf <- hexagon_all %>%
    select(HEX_ID, geometry) %>%
    inner_join(cube_data, by = "HEX_ID")
  
  return(cube_sf)
}

# Create cubes for each period
am_cube <- create_spacetime_cube("AM_TRIPS")
pm_cube <- create_spacetime_cube("PM_TRIPS")  
wkd_cube <- create_spacetime_cube("WKD_TRIPS")

# Check the cubes
cat("AM Cube dimensions:", nrow(am_cube), "records across", n_distinct(am_cube$MONTH_NUM), "months\n")
cat("PM Cube dimensions:", nrow(pm_cube), "records across", n_distinct(pm_cube$MONTH_NUM), "months\n")
cat("Weekend Cube dimensions:", nrow(wkd_cube), "records across", n_distinct(wkd_cube$MONTH_NUM), "months\n")
```

#### 7.2 Calculate Gi\* Statistics Over Time

```{r}
# Function to calculate Gi* for each time slice - FIXED
calculate_gistar_timeseries <- function(cube_data) {
  
  # Get unique months
  months <- unique(cube_data$MONTH_NUM)
  
  results <- list()
  
  for(month_idx in seq_along(months)) {
    current_month <- months[month_idx]
    
    # Filter data for current month and drop geometry for joining
    month_data <- cube_data %>% 
      filter(MONTH_NUM == current_month) %>%
      st_drop_geometry() %>%
      select(HEX_ID, trips)
    
    # Start with hexagon geometry
    complete_data <- hexagon_all %>%
      select(HEX_ID, geometry) %>%
      # Join with month data, filling missing with 0
      left_join(month_data, by = "HEX_ID") %>%
      mutate(trips = replace_na(trips, 0))
    
    # Calculate Gi* for this month
    month_gistar <- complete_data %>%
      mutate(
        nb = st_contiguity(geometry),
        wt = st_weights(nb, style = "W", allow_zero = TRUE),
        gistar = local_gstar_perm(trips, nb, wt, nsim = 99)
      ) %>%
      unnest(gistar) %>%
      mutate(MONTH_NUM = current_month)
    
    results[[as.character(month_idx)]] <- month_gistar
  }
  
  # Combine all months
  final_result <- bind_rows(results)
  return(final_result)
}

# Now calculate Gi* time series for each period
cat("Calculating AM Gi* time series...\n")
am_gistar_ts <- calculate_gistar_timeseries(am_cube)

cat("Calculating PM Gi* time series...\n")
pm_gistar_ts <- calculate_gistar_timeseries(pm_cube)

cat("Calculating Weekend Gi* time series...\n")
wkd_gistar_ts <- calculate_gistar_timeseries(wkd_cube)

# Check the results
cat("AM Gi* time series:", nrow(am_gistar_ts), "records\n")
cat("PM Gi* time series:", nrow(pm_gistar_ts), "records\n")
cat("Weekend Gi* time series:", nrow(wkd_gistar_ts), "records\n")

# Preview the results
glimpse(am_gistar_ts)
```

#### 7.3  Perform EHSA Analysis

```{r}
# Perform simplified EHSA for 2 months
perform_ehsa_simple <- function(gistar_ts) {
  
  # Calculate change between months
  ehsa_results <- gistar_ts %>%
    st_drop_geometry() %>%
    select(HEX_ID, MONTH_NUM, gi_star, p_sim, cluster) %>%
    group_by(HEX_ID) %>%
    arrange(MONTH_NUM) %>%
    summarise(
      month1_gi = first(gi_star),
      month2_gi = last(gi_star),
      month1_sig = first(p_sim < 0.05),
      month2_sig = last(p_sim < 0.05),
      month1_cluster = first(cluster),
      month2_cluster = last(cluster),
      gi_change = month2_gi - month1_gi,
      .groups = 'drop'
    ) %>%
    # Classify based on 2-month pattern
    mutate(
      ehsa_category = case_when(
        # Intensifying: Was hot spot and becoming stronger
        month1_sig & month2_sig & month1_cluster == "High" & month2_cluster == "High" & gi_change > 0 ~ "Intensifying",
        # Emerging: Was not significant, now hot spot
        !month1_sig & month2_sig & month2_cluster == "High" ~ "Emerging",
        # Diminishing: Was hot spot, now weaker or not significant
        month1_sig & month1_cluster == "High" & (!month2_sig | gi_change < 0) ~ "Diminishing",
        # Persistent: Stable hot spot
        month1_sig & month2_sig & month1_cluster == "High" & month2_cluster == "High" & abs(gi_change) < 0.5 ~ "Persistent",
        # Sporadic: Inconsistent pattern
        month1_sig != month2_sig ~ "Sporadic",
        # Cold spot patterns (similar logic but for low clusters)
        month1_sig & month2_sig & month1_cluster == "Low" & month2_cluster == "Low" & gi_change < 0 ~ "Intensifying Cold",
        !month1_sig & month2_sig & month2_cluster == "Low" ~ "Emerging Cold",
        month1_sig & month1_cluster == "Low" & (!month2_sig | gi_change > 0) ~ "Diminishing Cold",
        month1_sig & month2_sig & month1_cluster == "Low" & month2_cluster == "Low" & abs(gi_change) < 0.5 ~ "Persistent Cold",
        TRUE ~ "No Clear Pattern"
      )
    )
  
  return(ehsa_results)
}

# Perform EHSA for each time period
cat("Performing EHSA for AM trips...\n")
am_ehsa <- perform_ehsa_simple(am_gistar_ts)

cat("Performing EHSA for PM trips...\n")
pm_ehsa <- perform_ehsa_simple(pm_gistar_ts)

cat("Performing EHSA for Weekend trips...\n")
wkd_ehsa <- perform_ehsa_simple(wkd_gistar_ts)

# Check results
cat("AM EHSA categories:\n")
table(am_ehsa$ehsa_category)

cat("\nPM EHSA categories:\n")
table(pm_ehsa$ehsa_category)

cat("\nWeekend EHSA categories:\n")
table(wkd_ehsa$ehsa_category)
```

```{r}
# Function to create EHSA maps
create_ehsa_map <- function(ehsa_data, title) {
  # Join EHSA results with hexagon geometry
  ehsa_sf <- hexagon_all %>%
    inner_join(ehsa_data, by = "HEX_ID")
  
  # Define color scheme
  tm_shape(mpsz_cl) +
    tm_polygons(fill = "#FFF4EA", alpha = 0.3) +
    tm_shape(ehsa_sf) +
    tm_polygons(
      fill = "ehsa_category",
      fill.scale = tm_scale_categorical(
        values = c(
          "Intensifying" = "red",
          "Emerging" = "orange", 
          "Persistent" = "darkred",
          "Diminishing" = "lightblue",
          "Sporadic" = "yellow",
          "Intensifying Cold" = "darkblue",
          "Emerging Cold" = "blue",
          "Persistent Cold" = "lightblue",
          "Diminishing Cold" = "pink",
          "No Clear Pattern" = "grey80"
        )
      ),
      fill.legend = tm_legend(title = "EHSA Category", position = c("right", "bottom"))
    ) +
    tm_borders(lwd = 0.1) +
    tm_layout(
      main.title = title,
      bg.color = "#D0DDD0",
      frame = FALSE
    )
}

# Create EHSA maps
cat("Creating EHSA maps...\n")
am_ehsa_map <- create_ehsa_map(am_ehsa, "EHSA - Weekday AM Trips (Aug-Sep 2025)")
pm_ehsa_map <- create_ehsa_map(pm_ehsa, "EHSA - Weekday PM Trips (Aug-Sep 2025)") 
wkd_ehsa_map <- create_ehsa_map(wkd_ehsa, "EHSA - Weekend Trips (Aug-Sep 2025)")

# Display maps
am_ehsa_map
pm_ehsa_map  
wkd_ehsa_map
```

#### 7.4 EHSA Summary Statistics

```{r}
# Create summary tables
ehsa_summary <- function(ehsa_data, period_name) {
  summary_table <- ehsa_data %>%
    count(ehsa_category) %>%
    mutate(percentage = round(n / sum(n) * 100, 1),
           period = period_name) %>%
    arrange(desc(n))
  
  return(summary_table)
}

# Combine summaries
all_ehsa_summary <- bind_rows(
  ehsa_summary(am_ehsa, "AM Weekday"),
  ehsa_summary(pm_ehsa, "PM Weekday"),
  ehsa_summary(wkd_ehsa, "Weekend")
)

# Display summary
cat("EHSA Category Distribution:\n")
kable(all_ehsa_summary, caption = "EHSA Category Distribution (2-Month Analysis)")

# Cross-tabulation
ehsa_crosstab <- all_ehsa_summary %>%
  select(period, ehsa_category, n) %>%
  pivot_wider(names_from = period, values_from = n, values_fill = 0)

cat("\nEHSA Pattern Distribution Across Time Periods:\n")
kable(ehsa_crosstab, caption = "EHSA Pattern Distribution Across Time Periods")
```

#### 7.5 Key Locations Analysis

```{r}
# Identify key changing locations
key_changes <- bind_rows(
  am_ehsa %>% 
    filter(ehsa_category %in% c("Intensifying", "Emerging", "Diminishing")) %>%
    mutate(period = "AM"),
  pm_ehsa %>% 
    filter(ehsa_category %in% c("Intensifying", "Emerging", "Diminishing")) %>%
    mutate(period = "PM"),
  wkd_ehsa %>% 
    filter(ehsa_category %in% c("Intensifying", "Emerging", "Diminishing")) %>%
    mutate(period = "Weekend")
)

if(nrow(key_changes) > 0) {
  cat("Key changing locations found:\n")
  print(kable(key_changes))
  
  # Visualize these key locations
  key_locations_sf <- hexagon_all %>%
    inner_join(key_changes, by = "HEX_ID")
  
  key_changes_map <- tm_shape(mpsz_cl) +
    tm_polygons(fill = "#FFF4EA", alpha = 0.3) +
    tm_shape(key_locations_sf) +
    tm_polygons(
      fill = "ehsa_category",
      fill.scale = tm_scale_categorical(
        values = c("Intensifying" = "red", "Emerging" = "orange", "Diminishing" = "blue")
      ),
      fill.legend = tm_legend(title = "Key Changes")
    ) +
    tm_facets(by = "period", ncol = 1) +
    tm_layout(
      main.title = "Key Emerging Hot Spot Changes Across Periods",
      bg.color = "#D0DDD0"
    )
  
  print(key_changes_map)
} else {
  cat("No significant intensifying, emerging, or diminishing patterns detected in 2-month data.\n")
}
```

#### 7.6 Temporal Trend Visualization

```{r}
# Plot time series for selected hexagons with interesting patterns
interesting_categories <- c("Intensifying", "Emerging", "Diminishing")
interesting_hexagons <- am_ehsa %>%
  filter(ehsa_category %in% interesting_categories) %>%
  pull(HEX_ID)

# If we have interesting patterns, plot them
if(length(interesting_hexagons) > 0) {
  
  # Select top 6 for visualization
  plot_hexagons <- head(interesting_hexagons, 6)
  
  # Prepare plot data
  trend_plot_data <- am_gistar_ts %>%
    filter(HEX_ID %in% plot_hexagons) %>%
    st_drop_geometry() %>%
    select(HEX_ID, MONTH_NUM, gi_star, p_sim, cluster) %>%
    left_join(am_ehsa %>% select(HEX_ID, ehsa_category), by = "HEX_ID") %>%
    mutate(
      Month = case_when(
        MONTH_NUM == 1 ~ "August",
        MONTH_NUM == 2 ~ "September"
      ),
      Month = factor(Month, levels = c("August", "September")),
      significant = p_sim < 0.05
    )
  
  # Create trend plot
  trend_plot <- ggplot(trend_plot_data, aes(x = Month, y = gi_star, group = HEX_ID, color = ehsa_category)) +
    geom_line(size = 1.2) +
    geom_point(aes(shape = significant), size = 3) +
    geom_text(aes(label = ifelse(significant, cluster, "")), 
              vjust = -1, size = 3, color = "black") +
    facet_wrap(~HEX_ID, scales = "free_y", ncol = 2) +
    labs(
      title = "Gi* Statistic Trends for Selected Hexagons (AM Peak)",
      subtitle = "Labels show cluster type (High/Low) when statistically significant (p < 0.05)",
      x = "Month",
      y = "Gi* Statistic",
      color = "EHSA Category",
      shape = "Statistically\nSignificant"
    ) +
    theme_minimal() +
    scale_color_manual(values = c("Intensifying" = "red", 
                                 "Emerging" = "orange",
                                 "Diminishing" = "blue")) +
    scale_shape_manual(values = c("TRUE" = 16, "FALSE" = 1)) +
    theme(
      panel.grid.major = element_line(color = "grey80"),
      panel.grid.minor = element_blank(),
      strip.background = element_rect(fill = "lightblue", color = "grey50")
    )
  
  print(trend_plot)
  
} else {
  cat("No intensifying, emerging, or diminishing patterns to plot.\n")
}
```

#### 7.7 Comparative EHSA Analysis

```{r}
# Create a comprehensive comparison table
ehsa_comparison <- bind_rows(
  am_ehsa %>% mutate(period = "AM Peak") %>% select(HEX_ID, period, ehsa_category),
  pm_ehsa %>% mutate(period = "PM Peak") %>% select(HEX_ID, period, ehsa_category),
  wkd_ehsa %>% mutate(period = "Weekend") %>% select(HEX_ID, period, ehsa_category)
) %>%
  pivot_wider(names_from = period, values_from = ehsa_category, values_fill = "No Clear Pattern")

cat("EHSA Pattern Comparison Across Time Periods:\n")
kable(head(ehsa_comparison, 10), caption = "Sample of Hexagon EHSA Patterns Across Time Periods")

# Count patterns by period
pattern_summary <- ehsa_comparison %>%
  summarise(
    `AM Intensifying/Emerging` = sum(`AM Peak` %in% c("Intensifying", "Emerging")),
    `PM Intensifying/Emerging` = sum(`PM Peak` %in% c("Intensifying", "Emerging")),
    `Weekend Intensifying/Emerging` = sum(`Weekend` %in% c("Intensifying", "Emerging")),
    `AM Diminishing` = sum(`AM Peak` == "Diminishing"),
    `PM Diminishing` = sum(`PM Peak` == "Diminishing"),
    `Weekend Diminishing` = sum(`Weekend` == "Diminishing")
  )

cat("\nSummary of Key EHSA Patterns:\n")
kable(pattern_summary, caption = "Count of Key EHSA Patterns by Time Period")
```

#### 7.8 EHSA Insights and Interpretation

```{r}
# Generate insights based on EHSA results
cat("\n=== EHSA ANALYSIS INSIGHTS ===\n\n")

# Overall patterns
cat("1. OVERALL TEMPORAL PATTERNS:\n")
cat("   Based on 2 months of data (Aug-Sep 2025), the EHSA reveals:\n")
cat("   - AM Peak:", sum(am_ehsa$ehsa_category %in% c("Intensifying", "Emerging")), "areas showing growth\n")
cat("   - PM Peak:", sum(pm_ehsa$ehsa_category %in% c("Intensifying", "Emerging")), "areas showing growth\n")  
cat("   - Weekend:", sum(wkd_ehsa$ehsa_category %in% c("Intensifying", "Emerging")), "areas showing growth\n\n")

# Key findings
cat("2. KEY FINDINGS:\n")

# AM patterns
am_growth <- am_ehsa %>% filter(ehsa_category %in% c("Intensifying", "Emerging"))
if(nrow(am_growth) > 0) {
  cat("   - AM Commute: Growth detected in", nrow(am_growth), "locations\n")
}

# PM patterns  
pm_growth <- pm_ehsa %>% filter(ehsa_category %in% c("Intensifying", "Emerging"))
if(nrow(pm_growth) > 0) {
  cat("   - PM Commute: Growth detected in", nrow(pm_growth), "locations\n")
}

# Weekend patterns
wkd_growth <- wkd_ehsa %>% filter(ehsa_category %in% c("Intensifying", "Emerging"))
if(nrow(wkd_growth) > 0) {
  cat("   - Weekend Travel: Growth detected in", nrow(wkd_growth), "locations\n")
}

# Diminishing patterns
am_decline <- am_ehsa %>% filter(ehsa_category == "Diminishing")
pm_decline <- pm_ehsa %>% filter(ehsa_category == "Diminishing")  
wkd_decline <- wkd_ehsa %>% filter(ehsa_category == "Diminishing")

if(nrow(am_decline) + nrow(pm_decline) + nrow(wkd_decline) > 0) {
  cat("   - Declining activity found in", nrow(am_decline) + nrow(pm_decline) + nrow(wkd_decline), "locations\n")
}

cat("\n3. PLANNING IMPLICATIONS:\n")
cat("   - Monitor growing areas for potential service enhancements\n")
cat("   - Investigate declining areas for underlying causes\n") 
cat("   - Consider temporal variations in resource allocation\n")
cat("   - Note: 2-month analysis provides initial insights; longer time series recommended for robust EHSA\n")
```

### **Transport Service Optimization**

1.  **Resource Reallocation**: Shift capacity from "Diminishing" to "Intensifying/Emerging" areas

2.  **Peak-Hour Strategy**: Different service patterns needed for AM vs PM peaks

3.  **Weekend Services**: Enhanced connectivity to leisure destinations

## 8. Conclusion

This analysis of Singapore's bus network reveals a system characterized by strong, organized spatial patterns rather than random passenger distribution. The methodological approach, progressing from basic mapping to advanced statistical clustering, provides a clear and evidence-based understanding of urban mobility.

**Key Findings from the Analytical Process:**

1.  **Spatial Distribution Analysis (Hexagon Mapping):** The initial aggregation of bus stops into hexagonal zones provided a clear visual foundation. It immediately revealed that passenger demand is not uniform but concentrates in specific, identifiable areas across the island.

2.  **Spatial Autocorrelation (Global and Local Moran's I):** Statistical testing confirmed that the observed clustering is significant and not random. The fact that areas of high demand are surrounded by other high-demand areas (and likewise for low-demand areas) confirms that passenger movements follow predictable spatial structures.

3.  **Hot Spot Analysis (Getis-Ord Gi\*):** This technique successfully pinpointed the exact locations of statistically significant hotspots and cold spots. The alignment of these zones with known urban centers and transport nodes validates the current network design while highlighting areas of unexpected high or low usage.

4.  **Emerging Hot Spot Analysis (EHSA):** By incorporating the time dimension, the study demonstrated how these clusters are evolving. The identification of "Intensifying," "Emerging," and "Diminishing" areas provides a dynamic view of demand shifts, crucial for forward-looking planning.

**Interpretation and Strategic Implications:**

The findings indicate that Singapore's bus network efficiently serves a spatially structured demand pattern. The clear distinction between weekday commute corridors and weekend leisure destinations underscores a successful alignment with the city's land-use patterns.

From a planning perspective, the consistent identification of key clusters across multiple analytical methods offers a robust evidence base for decision-making. This enables a shift from reactive service adjustments to a more proactive and strategic approach. Resources can be prioritized with greater confidence, targeting investments and service enhancements toward zones of growing demand, while optimizing or re-allocating resources from areas of declining use.

In conclusion, this multi-layered analytical framework transforms complex mobility data into actionable intelligence. It provides a reliable foundation for ensuring that Singapore's bus network continues to evolve in step with the changing needs of its population, enhancing both efficiency and service quality.
